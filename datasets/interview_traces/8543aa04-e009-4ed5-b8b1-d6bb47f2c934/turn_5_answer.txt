Yeah, so in the recent data transformation task, I encountered a bottleneck where a script was calculating derived metrics across millions of rows using row by row for you, which was incredibly slow, right? So to optimize this, I refactored the logic to use NumPy and Pandas vectorized operations, which allowed the CPU to perform the same operation on an entire arrays at once using the highly optimized C and Fortran code. Specifically, I was able to replace conditional logic and arithmetic operations, such as calculating tax-adjusted tools based on multiple column conditions, which are vectorized, NP.WAP statements, and direct column-wise mathematical operations. So this is how the shift eliminated those things.